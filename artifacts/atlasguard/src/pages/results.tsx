/**
 * AtlasGuard — Results Page
 *
 * Renders the final evaluation output from the pipeline:
 *   - Verdict band + move score
 *   - Lifestyle fit & financial fit scores
 *   - Top positive drivers
 *   - Tradeoffs
 *   - Sensitivity notes
 *   - Recommended next step
 *
 * TODO: Implement UI via Cursor
 */
export default function ResultsPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-muted-foreground">Results — TODO: Implement via Cursor</p>
    </div>
  );
}
