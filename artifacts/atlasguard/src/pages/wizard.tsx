/**
 * AtlasGuard — Wizard Page
 *
 * 4-step wizard for capturing user input:
 *   Step 1: Current city
 *   Step 2: Target city
 *   Step 3: Finances (income, housing, expenses)
 *   Step 4: Lifestyle preferences & weights
 *
 * TODO: Implement UI via Cursor
 */
export default function WizardPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-muted-foreground">Wizard — TODO: Implement via Cursor</p>
    </div>
  );
}
